# Project runtime deps (install into the uv-managed .venv, Python 3.11)
#   uv venv --python 3.11
#   uv pip install -r requirements.txt
numpy>=1.25
scipy>=1.11
trimesh>=4.0
open3d>=0.18
pybullet>=3.2     # physics piling for the bin-scene generator (data-generation-design.md §5)
